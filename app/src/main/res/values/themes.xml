<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackAndTaste" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorPrimary">@color/green_primary</item>
        <item name="android:statusBarColor">@color/green_primary</item>
        <item name="android:colorSecondary">@color/green_primary</item>
        <item name="android:windowSplashScreenBackground">@color/green_primary</item>
        <item name="android:navigationBarColor">@color/black</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="BottomNavigation" parent="Base.Theme.TrackAndTaste">
        <item name="itemBackground">@color/green_primary</item>
    </style>

    <style name="ftactionBtn">
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="Theme.TrackAndTaste" parent="Base.Theme.TrackAndTaste" />
</resources>