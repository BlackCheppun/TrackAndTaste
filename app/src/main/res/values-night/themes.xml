<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackAndTaste" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:background">@color/background</item>
        <item name="android:colorPrimary">@color/green_primary</item>
        <item name="android:statusBarColor">@color/background</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="BottomNavigation" parent="Base.Theme.TrackAndTaste">
        <item name="itemBackground">@color/background</item>
    </style>

    <style name="ftactionBtn" >
        <item name="backgroundTint">@color/background</item>
    </style>
</resources>